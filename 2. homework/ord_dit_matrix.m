function D = ord_dit_matrix(n)
K= zeros(1,1);
l= zeros(2,2);
m= zeros(4,4);
x= zeros(8,8);
if n == 0 
        K= zeros(1,1);
        D = K*(1/(4^n));
 elseif n== 1
        l(:,:) = [4*K(:,:) 4*K(:,:)+2;4*K(:,:)+3 4*K(:,:)+1];
        D = l*(1/(4^n));
    
end
% K= zeros(1,1,n+1);
% l= zeros(2,2,n+1);
% m= zeros(4,4,n+1);
% x= zeros(8,8,n+1);
% 
%     if n == 0 
%     K(:,:,1) = 0;
%     D = K*(1/(4^n));
%     elseif n== 1        
%       l(:,:,n+1) = [4*K(:,:,n) 4*K(:,:,n)+2;4*K(:,:,n)+3 4*K(:,:,n)+1];  
%       D = l(:,:,n+1)*(1/(4^n));
%       elseif n== 2        
%       m(:,:,n+1) = [4*l(:,:,n) 4*l(:,:,n)+2;4*l(:,:,n)+3 4*l(:,:,n)+1];  
%       D = m(:,:,n+1)*(1/(4^n));
%       elseif n== 3        
%       x(:,:,n+1) = [4*m(:,:,n) 4*m(:,:,n)+2;4*m(:,:,n)+3 4*m(:,:,n)+1];  
%       D = x(:,:,n+1)*(1/(4^n));
%     end
%     
l=[0 2;3 1];
m= [0,8,2,10;12,4,14,6;3,11,1,9;15,7,13,5];
    if n== 2
        m(:,:) = [4*l(:,:) 4*l(:,:)+2;4*l(:,:)+3 4*l(:,:)+1];
        D = m*(1/(4^n));
    elseif n== 3 
        x(:,:) = [4*m(:,:) 4*m(:,:)+2;4*m(:,:)+3 4*m(:,:)+1];
        D = x*(1/(4^n));
    end
        
end
